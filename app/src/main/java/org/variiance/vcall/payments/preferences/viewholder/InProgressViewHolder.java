package org.variiance.vcall.payments.preferences.viewholder;

import android.view.View;

import androidx.annotation.NonNull;

import org.variiance.vcall.payments.preferences.model.InProgress;
import org.variiance.vcall.util.adapter.mapping.MappingViewHolder;

public class InProgressViewHolder extends MappingViewHolder<InProgress> {

  public InProgressViewHolder(@NonNull View itemView) {
    super(itemView);
  }

  @Override
  public void bind(@NonNull InProgress model) { }
}
